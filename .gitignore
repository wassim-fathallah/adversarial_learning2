# Python
__pycache__/
**/__pycache__/
*.pyc
*.pyo

# Claude Code local settings
.claude/settings.local.json

# Virtual environments
.venv/
.venv_gpu/
venv/
env/

# Jupyter notebooks
*.ipynb
.ipynb_checkpoints/

# OS / IDE
.DS_Store
Thumbs.db
.vscode/
.idea/

# Datasets (too large — download from Dropbox, see README)
adversarial_fairness/datasets/
fair_fairness_benchmark/datasets/

# FFB junk
fair_fairness_benchmark/src/wandb/
fair_fairness_benchmark/img/
wandb/

# FFB results — all included in repo (small, final-step only)
# Nothing excluded here — all results/*.json are committed

# Run logs and error dumps
*.log
*.err

# Backup snapshots (memory swaps, insert-script .bak, etc.)
*.bak
*.bak_*
long_term_memory.backup_*.json

# Per-run training-curve plots (regenerated each run; paper figures are kept by name)
*_training_curves.png

# Downloaded dataset archive (setup.py fetches then deletes it)
datasets.zip

# WandB run dirs in any location
**/wandb/

# Local environment / secrets
.env
